#include "main.h"

/**
 * _abs - computes absolute value of integer
 *
 * @n: integer being passed to function
 *
 * Return: returns 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

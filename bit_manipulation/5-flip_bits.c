#include "main.h"

/**
 * flip_bits - returns number of bits needed to flip for one num to another
 * @n: starting bit
 * @m: ending bit
 *
 * Return: returns number of bits needed on success
 */

unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
}
